# CMS security boundary

The public patient website must not expose the staff CMS editor.

Future CMS administration must be deployed separately behind authentication/authorization; browser localStorage, hidden buttons, or client-side feature flags are not an access-control boundary.
